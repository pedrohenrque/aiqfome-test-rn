import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingHorizontal: 20,
    alignItems: 'center'
  },
  productList: {
    flex: 1,
    width: '100%',
    marginTop: 20,
    marginVertical: 50
  }
})
